<% include header %>
  
  <div class="container text-center">
  <nav>
    <h1>photo upload</h1>
    <div>upload photos</div>
  </nav>
      <div class="row list">
        <% for(var i = 0;i<photos.length;i++){ %>
            <div class="col-sm-12 col-md-4 ">
                <div><img src="<%= photos[i].path %>" alt=""></div>
                <h1><%= photos[i].name %></h1>
            </div>
       <% } %>
       </div>
  </div>
<% include footer %>
